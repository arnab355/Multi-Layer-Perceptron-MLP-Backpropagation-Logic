# Multi-Layer-Perceptron-MLP-Backpropagation-Logic
A fundamental deep learning project implementing a Multi-Layer Perceptron. It features a manually programmed forward pass, error calculation (MSE), and backward pass (Backpropagation) using NumPy to update weights and biases, solving non-linear logic problems.
